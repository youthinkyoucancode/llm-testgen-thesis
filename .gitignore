.venv/
__pycache__/
*.pyc
.env
.coverage
htmlcov/
.mutmut-cache
mutants/
.ipynb_checkpoints/
.pytest_cache/
# Contents, not the directory itself: git cannot re-include anything inside an
# excluded directory, and the aggregate tables below must stay tracked.
experiments/results/*
# The per-record JSONs stay out (they are large and live in Drive), but the
# aggregate tables Chapter 5 is written from are versioned: they are small,
# they diff, and the thesis must not depend on a Colab session being alive.
!experiments/results/analysis/
experiments/figures/
experiments/targets/
*.log

# Personal build log and plain-language companion. Kept locally, not part of
# the published artifact: both went stale once the campaign finished.
CHECKLIST.md
EXPLAINER.md
