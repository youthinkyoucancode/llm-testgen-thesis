"""Quality filters: parse, execute, coverage gain, mutation kill.

Each filter is independent and separately testable, so the evaluation can ablate
them one at a time and measure what each contributes.
"""
