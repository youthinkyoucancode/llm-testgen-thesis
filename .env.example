# Only needed if you switch to a closed model. The default open model (Ollama) needs nothing here.
# Copy this file to .env and fill in the key for whichever provider you use.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
